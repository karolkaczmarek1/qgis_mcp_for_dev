[project]
name = "qgis-mcp"
version = "0.1.1"
description = "Model Context Protocol (MCP) server for QGIS integration and automation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.3.0",
]
