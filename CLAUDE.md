@AGENTS.md

## Claude Code

- The `qgis` MCP server is registered in `.mcp.json`; approve it when Claude Code asks. Its tools appear as `mcp__qgis__*`.
- The MCP tools only work while the QGIS plugin server is running on `localhost:9876`.
